This is a port of libtommath to Plan9 APE.
Hopefully it will be possible to use as a replacement of gmp for certain projects.

mkfile generated by:

mkmk -s libtommath -a -B -I. -I/sys/include/ape -I/$objtype/include/ape -D_POSIX_SOURCE -D_SUSV2_SOURCE -D_BSD_EXTENSION -x demo -x etc -x logs -x mtest -x pics -x pre_gen -x tombc > mkfile

mkfile manually edited for installation of header files and proper library name.