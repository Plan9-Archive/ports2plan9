</$objtype/mkfile
LIB=liby.a$O
OFILES=libmai.$O libzer.$O 

</sys/src/cmd/mklib

CC=pcc
LD=pcc
CFLAGS=-c -I. -B -D_POSIX_SOURCE -D_SUSV2_SOURCE -D_BSD_EXTENSION -DPARSER="/sys/lib/yaccpar" 

libzer.$O: 
libmai.$O: 

nuke:V:
	mk clean
	rm -f $LIB
